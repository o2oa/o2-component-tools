{
  "name": "<%= projectPath %>",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "o2-deploy": "rsbuild build",
    "o2-build": "rsbuild build"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
